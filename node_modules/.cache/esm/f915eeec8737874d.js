_873‍.x([["getResponse",()=>getResponse],["getErrorResponse",()=>getErrorResponse]]);// import { Request } from 'express';

       const getResponse = (success, message, data = null) => {
  const response = { success, message, data };
  return response;
};

       const getErrorResponse = () => {
  const response = { success: false, message: 'Something went wrong', data: null };
  return response;
};